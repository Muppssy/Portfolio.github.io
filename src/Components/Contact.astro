---
const { contact } = Astro.props;
---
<style>
.icon{
    background: linear-gradient(to right, rgba(27, 253, 156, 0.1) 1%, transparent 40%, transparent 60%, rgba(27, 253, 156, 0.1) 100%);
    box-shadow: inset 0 0 10px rgba(27, 253, 156, 0.4), 0 0 9px 3px rgba(27, 253, 156, 0.1);
    color: #82ffc9;
}

</style>

<section id="contact" class="bg-gray-900">
    <div class="mx-auto max-w-7xl px-4 py-16 sm:px-6 lg:px-16 lg:py-20">
        <div class="mb-4">
            <div class="mb-6 max-w-3xl text-center text-white sm:text-center md:mx-auto md:mb-12">
                <h2
                    class="font-heading mb-4 font-bold tracking-tight text-3xl sm:text-5xl">
                    Intéressez ?
                </h2>
            </div>
        </div>
        <div class="flex items-stretch justify-center">
            <div class="grid md:grid-cols-2 ">
                <div class=" contact-info h-full ">
                    <p class="mt-3 mb-12 text-lg font-poppins text-white dark:text-slate-400">
                        N'hésitez pas à me contacter pour toute question, collaboration ou opportunité — je suis toujours ouvert à de nouvelles connexions et échanges.
                    </p>
                    <ul class="mb-6 md:mb-0">
                      {contact.map((contact) => (
                        <li class="flex">
                            <div class=" icon  flex h-10 w-10 items-center justify-center rounded text-gray-50">
                              <i class={contact.icon}></i>
                            </div>
                            <div class="ml-4 mb-4">
                                <h3 class="mb-2 text-lg font-medium leading-6 text-white">{contact.name}
                                </h3>
                                <p class="text-white">{contact.adress}</p>
                            </div>
                        </li>
                          ))}
                    </ul>
                </div>
                <div class=" contact-img max-w-6xl w-full p-2 md:p-4">
                    <img src="/img/collaboration.webp" alt="Illustration de collaboration représentant des personnes travaillant ensemble" aria-hidden="true">
                </div>
            </div>
        </div>
    </div>
  </section>